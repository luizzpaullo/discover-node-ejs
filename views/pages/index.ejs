<!DOCTYPE html>
<html lang="en">

<!-- Inclui o html que esta no head.ejs -->
<%- include('../partials/head') %> 

<body class="container">

    <!-- Incluir o HTML do HEADER.EJS -->
    <%- include ('../partials/header',{title: 'Titulo Dinâmico do Index'}) %> 

    <main>
        <div class="jumbotron">
            <h1>Node com EJS</h1>
            <p>Vamos aprender a trabalhar com arquivos EJS</p>
            <p><strong><%= subtitle %> </strong></p>
        </div>

        <ul class="jumbotron">
            <h2>Características do EJS</h2>
            <!-- forEach no EJS com HTML e JS -->
            <% qualitys.forEach(function(quality) { %>

                <li>
                    <strong><%= quality.title %> </strong> <%= quality.message %> 
                </li>

            <% }) %> 

        </ul>
    </main>

<!-- Inclui o HTML do FOOTER.EJS -->
    <%- include ('../partials/footer') %> 
</body>
</html>